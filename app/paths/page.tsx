import Nav from "@/components/Nav";
import { paths } from "@/lib/data";
export default function Paths(){return <><Nav/><main className="container section"><div className="pill">LEARNING PATHS</div><h1 className="section-title">Build your career path</h1><p className="muted">Curated learning journeys designed around real-world roles.</p><div className="path-grid" style={{marginTop:35}}>{paths.map((p,i)=><div className="card path" key={p.title}><span className="pill">PATH {String(i+1).padStart(2,"0")}</span><h2 style={{fontSize:23}}>{p.title}</h2><p className="muted" style={{lineHeight:1.6}}>{p.desc}</p><div style={{fontSize:12,color:"#aab3c7"}}>{p.courses} courses · {p.hours} · {p.level}</div><button className="btn btn-secondary" style={{marginTop:18}}>View path →</button></div>)}</div></main></>}